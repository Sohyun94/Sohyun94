package 소수찾기;

class Solution {
	public int solution(int number) {
		// 정답
		int answer = 0;
		// 소수 판별 boolean 함수
		boolean check = true;
		
		//입력받은 수 number까지 반복문
		// i보다 작은 수(j)로 계속 나누며 나머지가 0이면
		// 소수가 아닌 것이므로 반복문 빠져나가기
		for(int i = 2; i <= number; i++) {
			for(int j = 2 ; j < i; j++)
		}

		return answer;
	}
}